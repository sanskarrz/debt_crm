{
  "name": "debt-recovery-crm",
  "version": "1.0.0",
  "description": "Debt Recovery Call Center CRM with SIP Trunk & GSM Gateway Integration",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:dialer\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:dialer": "cd dialer && npm run dev",
    "build": "npm run build:frontend && npm run build:backend && npm run build:dialer",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:dialer": "cd dialer && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../dialer && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
